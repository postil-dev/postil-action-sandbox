name: Postil Action smoke review

on:
  workflow_dispatch:
    inputs:
      pr:
        description: Pull request number to review
        required: true
        type: number

jobs:
  review:
    name: Review fixture
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      checks: write
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Run postil reviewer
        # postil-action requires a full 40-hex commit SHA of postil-dev/postil-cli
        # (cli-ref), not a tag or branch; pin to postil-action's own main too so
        # a fixture failure can't silently start exercising a different action
        # version than intended. Bump both when postil-cli/postil-action cut a
        # new release this fixture should track.
        uses: postil-dev/postil-action@9c8cf2c2f650f5946774c6d01626da507836b418
        with:
          pr: ${{ inputs.pr }}
          cli-ref: dd1381381d4791475a277333837394f2f5032d27
          cli-release: v0.6.0
          api-key: ${{ secrets.OPENROUTER_API_KEY }}
