name: Postil Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    name: Postil Review
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run postil reviewer
        uses: postil-dev/postil@9298df9a246d821dcc66784e78203a4e8dc9e7d0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
